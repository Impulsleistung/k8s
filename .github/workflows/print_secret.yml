name: Print Secret

on:
  push:
    branches:
      - main

jobs:
  print-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Print secret
        run: echo "The secret is ${{ secrets.USSER }}"
