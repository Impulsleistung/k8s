apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aaa-chart.fullname" . }}
  labels:
    app: aaa-chart
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: aaa-chart
  template:
    metadata:
      labels:
        app: aaa-chart
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}