apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ddd-chart.fullname" . }}
  labels:
    app: ddd-chart
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: ddd-chart
  template:
    metadata:
      labels:
        app: ddd-chart
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}