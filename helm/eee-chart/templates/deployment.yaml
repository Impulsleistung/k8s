apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eee-chart.fullname" . }}
  labels:
    app: eee-chart
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: eee-chart
  template:
    metadata:
      labels:
        app: eee-chart
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}