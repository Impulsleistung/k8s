apiVersion: v1
kind: Service
metadata:
  name: ddd-service
  labels:
    app: ddd
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: ddd
